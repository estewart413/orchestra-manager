import React from 'react';
import { Text, StyleSheet } from 'react-native';

const Background = () => {
    return <Text style={StyleSheet.textStyle}>Background Screen</Text>
};

const styles = StyleSheet.create({
    textStyle: {
        fontSize: 20
    }
});

export default ComponentsScreen;